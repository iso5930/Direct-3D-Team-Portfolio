1. Enable 함수 - Enable을 true로 만든다.

2. Disable 함수 - Enable을 false로 만든다.

3. IsEnable 함수 - Enable을 반환한다.

// 여기가진 쉬워

4. Move 함수 - 변경해야하는 데이터는 Mode와 Dst이다.
만약, 스레드가 돌고있다면 false를 반환한다. 
만약, 스레드가 안돌고 있다면 true를 반환한다.

5. Stop 함수 - Next = Path의 Size를 넣어 다음 경로가 없도록 만든다.
만약, Mode가 1이라면 false를 반환한다.
만약, Mode가 1이 아니라면 true를 반환한다. Mode를 2로 변경한다.

6. IsMove 함수 - Mode가 1이면 